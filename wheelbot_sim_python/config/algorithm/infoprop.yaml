name: "infoprop"

num_timesteps: 10000000
episode_length: 2000

# buffer config
max_physics_replay_size: 100000
min_physics_replay_size: 10000
max_model_replay_size: 10000000
min_model_replay_size: 0

# agent training config
agent_learning_rate: 0.0003
agent_batch_size: 1024
agent_hidden_layer_sizes: [256, 256]
agent_layer_norm: False
grad_updates_per_model_step: 1
num_training_steps_per_model_train: 1000
num_resampling_epochs: 10
target_entropy: -1
tau: 0.005
tune_entropy: True
alpha: 0.1
reset_agent_per_trial: False
reset_model_replay_buffer: False
reset_model_per_trial: False

# model training config
model_learning_rate: 0.001
model_weight_decay: 0.00001
model_batch_size: 1024
model_hidden_layer_sizes: [512, 512]
model_layer_norm: False
patience: 10            # model early stopping: epochs without val-loss improvement
min_log_var: -10
max_log_var: 0.5

# rollout config
num_model_envs: 1000
max_rollout_length: 1000
lower_quantile: 0.01    # quantile of real-data entropy → λ₂ (accumulated cutoff)
upper_quantile: 1.0     # quantile of real-data entropy → λ₁ (per-step cutoff)
action_repeat: 1

# miscellaneous
num_real_train_envs: 5
num_real_eval_envs: 1000
discounting: 0.999
num_trials: 20
normalize_observations: True